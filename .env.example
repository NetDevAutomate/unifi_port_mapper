# UniFi Management CLI Configuration
# Copy to: ~/.config/unifi_management_cli/prod.env (XDG standard)

# ==============================================
# UniFi Controller Connection
# ==============================================

# Controller URL (required)
UNIFI_URL=https://unifi.local:8443

# Site name (default: "default")
UNIFI_SITE=default

# ==============================================
# Authentication (choose one method)
# ==============================================

# Method 1: API Token (recommended)
UNIFI_CONSOLE_API_TOKEN=your_api_token_here

# Method 2: Username/Password
# UNIFI_USERNAME=admin
# UNIFI_PASSWORD=your_password_here

# ==============================================
# Connection Settings
# ==============================================

# SSL verification (true/false, default: false for self-signed certs)
UNIFI_VERIFY_SSL=false

# Connection timeout in seconds (default: 10)
UNIFI_TIMEOUT=10

# Maximum retry attempts (default: 3, range: 1-10)
UNIFI_MAX_RETRIES=3

# Retry delay in seconds (default: 1.0, range: 0.1-10.0)
UNIFI_RETRY_DELAY=1.0

# ==============================================
# Output Preferences (NEW - Optional)
# ==============================================

# Default diagram format if --format not specified
# Options: png, svg, html, mermaid, dot
# Default: png
UNIFI_DEFAULT_FORMAT=png

# Default output directory for reports (optional)
# If not set, uses ./reports in current directory
# UNIFI_OUTPUT_DIR=~/Documents/network-reports

# Default directory for diagrams (optional)
# If not set, uses ./diagrams in current directory
# UNIFI_DIAGRAM_DIR=~/Documents/network-diagrams

# ==============================================
# Usage Examples
# ==============================================

# With this config file at ~/.config/unifi_management_cli/prod.env:
#
# 1. Use all defaults from config:
#    unifi-mapper
#
# 2. Override format:
#    unifi-mapper --format svg
#
# 3. Override output locations:
#    unifi-mapper --output ~/custom/report.md --diagram ~/custom/diagram.png
#
# 4. Dry run (no port updates):
#    unifi-mapper --dry-run
#
# 5. Include connected devices:
#    unifi-mapper --connected-devices

# ==============================================
# Security Notes
# ==============================================

# 1. Set restrictive permissions:
#    chmod 600 ~/.config/unifi_management_cli/*.env
#
# 2. Never commit config files to git
#
# 3. Use API tokens instead of passwords when possible
#
# 4. Rotate tokens regularly

# ==============================================
# XDG Base Directory Specification
# ==============================================

# This file follows XDG standards:
# - Config: ~/.config/unifi_management_cli/
# - Data: ~/.local/share/unifi_management_cli/ (future)
# - Cache: ~/.cache/unifi_management_cli/ (future)
#
# Override with environment variable:
# export XDG_CONFIG_HOME=/custom/location
