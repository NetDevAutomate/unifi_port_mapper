#!/bin/bash
# Fix UniFi port name persistence issues

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$PROJECT_ROOT"
python src/scripts/fix_port_persistence.py "$@"
