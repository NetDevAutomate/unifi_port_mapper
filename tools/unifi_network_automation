#!/bin/bash
# Wrapper script for UniFi network automation

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT"
python src/scripts/unifi_network_automation.py "$@"
